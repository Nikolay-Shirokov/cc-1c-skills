{
  "name": "Дескриптор формы Forms/<Имя>.xml в версии выгрузки — Form распознан, Name и FormType проверены, проверка 24 OK",
  "setup": "fixture:dump-version-match",
  "params": { "objectPath": "Catalogs/Товары/Forms/ФормаЭлемента.xml" },
  "noSnapshot": "meta-validate только читает и печатает — эталон зафиксировал бы фикстуру, а не выход проверяемого навыка; проверяется stdout",
  "expect": {
    "stdoutContains": ["=== Validation OK: Form.ФормаЭлемента (4 checks) ==="],
    "stdoutNotContains": ["differs from the dump", "Unrecognized metadata type", "[ERROR]"]
  },
  "skipPlatformVerify": "фикстура — фрагмент выгрузки без Ext/Form.xml и модулей: платформа его не загрузит, проверяется вывод валидатора"
}
